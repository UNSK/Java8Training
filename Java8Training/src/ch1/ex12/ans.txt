安全ではない。Streamメソッドを実装していた場合、コンパイルエラーとなる。
バイナリ互換性については…試していないので自信がないが以下のとおりになりそう。

独自のstreamメソッドを実装している、ListImplクラスをJava 8以前でJARにする。
Java 8のJVMで動かすと次のとおりになる。
List型のインスタンスの場合、Java 8 のstreamが呼ばれる。
ListImpl型のインスタンスの場合、独自のstreamが呼ばれる。
